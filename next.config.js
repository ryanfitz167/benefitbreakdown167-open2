// FILE: next.config.js  (DELETE next.config.mjs if it exists)
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Keep minimal for Next 14. Remove Next 15-only keys like outputFileTracingRoot.
};
module.exports = nextConfig;
